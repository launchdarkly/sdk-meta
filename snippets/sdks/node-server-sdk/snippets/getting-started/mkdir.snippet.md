---
id: node-server-sdk/getting-started/mkdir
sdk: node-server-sdk
kind: bootstrap
lang: shell
description: Create the project directory and a package.json.
ld-application:
  slot: mkdir
---

Create a new directory and create a `package.json` file:

```shell
mkdir hello-node-server && cd hello-node-server && npm init
```
