---
id: node-client-sdk/getting-started/mkdir
sdk: node-client-sdk
kind: bootstrap
lang: shell
description: Create the project directory and a package.json.
ld-application:
  slot: mkdir
---

Create a new directory and create a `package.json` file:

```shell
mkdir hello-node-client && cd hello-node-client && npm init
```
