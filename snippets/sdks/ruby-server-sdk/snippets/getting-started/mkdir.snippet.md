---
id: ruby-server-sdk/getting-started/mkdir
sdk: ruby-server-sdk
kind: bootstrap
lang: shell
description: Create the project directory and initialize a Gemfile.
ld-application:
  slot: mkdir
---

Create a new directory for your application:

```shell
mkdir hello-ruby && cd hello-ruby && bundle init
```
