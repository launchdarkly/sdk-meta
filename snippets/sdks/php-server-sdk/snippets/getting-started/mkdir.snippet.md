---
id: php-server-sdk/getting-started/mkdir
sdk: php-server-sdk
kind: bootstrap
lang: shell
description: Create the project directory and install Composer.
ld-application:
  slot: mkdir
---

Create a new directory and install [Composer](https://getcomposer.org/):

```shell
mkdir hello-php && cd hello-php && curl -sS https://getcomposer.org/installer | php
```
