package version

// Version is the sdk-snippets release this binary was built from.
// Release-please bumps the literal below via the snippets package's
// `extra-files` rule in release-please-config.json — keep the
// `x-release-please-version` annotation on the same line.
const Version = "0.1.0" // x-release-please-version
